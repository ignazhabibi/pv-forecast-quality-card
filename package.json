{
  "name": "pv-forecast-quality-card",
  "version": "0.1.1",
  "description": "A Home Assistant Sections card for understandable PV forecast quality comparisons.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "build": "tsc --noEmit && vite build",
    "check": "tsc --noEmit && vitest run",
    "test": "vitest run",
    "dev": "vite --host 127.0.0.1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ignazhabibi/pv-forecast-quality-card.git"
  },
  "license": "MIT",
  "dependencies": {
    "lit": "^3.3.1"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.2.2",
    "vitest": "^4.0.9"
  }
}
